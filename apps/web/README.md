# Web Report Reader

The web app is a lightweight report reader, not the comparison engine.

Initial pages:

- comparison setup
- schema inspection
- key/exclusion selection
- run summary
- column stats
- detailed differences
- missing rows
- export/download page

Keep the frontend dependent on backend DTOs and report contracts only.
