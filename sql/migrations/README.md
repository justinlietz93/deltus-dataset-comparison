# SQL Migrations

No required database objects are installed for `0.1.0`.

The current SQL Server adapter is read-only and uses metadata queries plus bounded table reads. Future `0.2.x` work can add installable stored procedures or snapshot tables here when the server-side execution path is hardened.
